# GoFilm 项目日志

## 2026-08-05
- 创建影院首页模板 `views/index.html`，沿用 bookstore 架构风格：Go template + jQuery + Ajax
- v1 布局：header（深色影院风导航） / main（搜索栏 + 分组筛选 + 电影卡片列表 + 分页） / bottom
- v2 重构（参照用户截图）：深色 topbar（logo+搜索+登录/注册）+ subnav（首页/分类/影院/我的/管理员）+ 筛选区（标签/地区下拉 + 横向分类 chips）+ 3列大海报网格（无分页）
- 配色：黑底 #1a1a1a + 多彩渐变（#ff6b6b → #ffa500 → #ffd93d → #4ecdc4 → #45b7d1 → #a78bfa）
- 关键样式：渐变边框（通过 `background-image` + `background-clip: border-box` 实现），hover 时海报放大 + 渐变描边
- 模板变量：IsLogin, Username, Identity, Movies, Categories, Tags, CategoryID, TagID, Region, Keyword
- 创建 `views/static/css/style.css`，覆盖首页、登录页、通用表格样式
- 复制 jQuery 1.7.2 到 `views/static/script/`
- 交互：搜索、分类切换、标签/地区下拉、电影点击进入详情、注销
- 创建 `model/seed.go`：35部电影种子数据（Go硬编码），覆盖12种类型+5地区
- 重建 `sql.sql`：完整建库脚本（users/sessions/movies三表）+ 管理员/测试账号 + 35条电影INSERT
  - 海报使用 Lorem.space API（不同hash=不同图，页面即刻可见真实效果）
  - 每部电影附带简介/类型/地区，全部对应分类筛选和地区下拉
- Lorem.space API 在本地网络不可用，改用 Python+Pillow 本地生成 35 张海报
  - `.workbuddy/temp/gen_posters.py`：脚本（13 种类型配色 + 渐变背景 + 中文标题 + 简介 + 装饰圆 + 编号）
  - `.workbuddy/temp/movies/`：原始 35 张图
  - `.workbuddy/temp/movie_posters.zip`：压缩包 304KB
  - 临时方案，**真实项目建议手动替换成正版海报**（按 ID 重命名后放 `static/img/movies/`）
- 最终方案：通过 TMDB 批量抓取 35 部真实电影海报
  - 流程：TMDB 搜索页 → 提取 movie ID → 详情页 → 提取 `/t/p/w500/` 海报 → 下载
  - 海报保存在 `views/static/img/movies/01_战狼2.jpg ~ 35_铃芽之旅.jpg`
  - 同步更新 `model/seed.go` 和 `sql.sql` 的 `image_path` 指向本地路径
  - 抓取脚本：`.workbuddy/temp/fetch_posters.py` (主) + `retry_posters.py` (补漏)
  - 关键点：英文搜索词对部分中文片失效（如 Dying to Survive 2018 返回空），改用片名原语言（中文/日文）成功率更高
  - 3 次重试后 35/35 全部成功（部分通过直接 TMDB movie ID 访问）
- 创建登录页 `views/pages/user/login.html`：沿用 bookstore 结构，影院暗金主题风格
- 创建注册页 `views/pages/user/regist.html`：含用户名/密码/确认密码/邮箱 + Ajax 重名检测
- CSS 登录页样式更新：header 暗金渐变 / banner 深棕底 / 按钮暗金系 / sidebar 左侧大标题
- 创建登录成功页 `views/pages/user/login_success.html`：显示用户名 + "欢迎回来" + 跳转首页
- 创建注册成功页 `views/pages/user/regist_success.html`：成功提示 + 前往登录/首页两个入口
- 修复 index.html 页面不显示问题：model.Page 增补字段 + movie.Rating/Duration + controller 传参 + isLogin JS 变量
